# .github/workflows/test.yml
name: QA Automation Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# GitHub Pages 배포 권한 (Actions가 Pages에 배포하기 위해 필요)
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📦 코드 체크아웃
      uses: actions/checkout@v4
    
    - name: 🐍 Python 3.12 설치
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: 🌐 Chrome 설치
      uses: browser-actions/setup-chrome@v1
    
    - name: 📥 의존성 설치
      run: |
        pip install -r requirements.txt
    
    - name: 🧪 테스트 실행
      env:
        CI: true
        ADMIN1_USERNAME: ${{ secrets.ADMIN1_USERNAME }}
        ADMIN1_PASSWORD: ${{ secrets.ADMIN1_PASSWORD }}
        ADMIN2_USERNAME: ${{ secrets.ADMIN2_USERNAME }}
        ADMIN2_PASSWORD: ${{ secrets.ADMIN2_PASSWORD }}
        ADMIN3_USERNAME: ${{ secrets.ADMIN3_USERNAME }}
        ADMIN3_PASSWORD: ${{ secrets.ADMIN3_PASSWORD }}
      run: |
        pytest tests/ --alluredir=allure-results
       
    - name: 📊 Allure 리포트 생성 (이력 포함)
      uses: simple-elf/allure-report-action@master
      if: always()
      with:
        allure_results: allure-results
        allure_history: allure-history
        keep_reports: 20
     
    - name: 📤 Pages artifact 업로드
      uses: actions/upload-pages-artifact@v3
      if: always()
      with:
        path: allure-history   
        
    - name: 🚀 GitHub Pages에 배포
      uses: actions/deploy-pages@v4
      if: always()